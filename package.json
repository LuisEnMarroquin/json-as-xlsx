{
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "dev": "lerna run --parallel --stream dev",
    "build": "lerna run --stream build"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "lerna": "^6.1.0"
  }
}
